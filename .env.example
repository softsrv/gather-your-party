# Externally hosted Postgres; keep the TLS settings required by your provider.
DATABASE_URL=postgres://user:pass@host:5432/db?sslmode=require
# High-entropy secret securing sessions by signing the session cookie.
SESSION_SECRET=replace-with-a-random-secret
# Public origin for OpenID realm/return_to, including behind a TLS-terminating proxy.
# Use the public HTTPS origin (no trailing slash) when deployed; cookies are Secure.
APP_BASE_URL=http://localhost:8080
# Steam Web API key used for profile/game requests.
STEAM_API_KEY=replace-with-your-steam-api-key
# Port only (the server adds the colon).
LISTEN_ADDR=8080
